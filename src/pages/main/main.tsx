import React from 'react';

import { PageContainer } from 'src/container';

import { DetailsForm } from './sections';

const MainPage: React.FC = () => {
  return (
    <PageContainer>
      <DetailsForm />
    </PageContainer>
  );
};

export default MainPage;
